# Proxmox API credentials
# These values should be encrypted with ansible-vault
vault_proxmox_api_password: "your_proxmox_password_here"

# Host-specific credentials
# For accessing individual hosts with different credentials
host_credentials:
  pve-test:
    ansible_host: 192.168.1.47
    ansible_user: root
    ansible_password: "your_pve_test_password_here"
    api_password: "your_api_password_for_pve_test"

# Additional credentials as needed
