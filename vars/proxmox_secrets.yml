# Proxmox API credentials
# These values should be encrypted with ansible-vault
vault_proxmox_api_password: "" #changing in the future. Testing/wip

# Host-specific credentials
# For accessing individual hosts with different credentials
host_credentials:
  pve-test:
    ansible_user: root
    ansible_password: "" #changing in the future. Testing/wip
    api_password: "" #changing in the future. Testing/wip
